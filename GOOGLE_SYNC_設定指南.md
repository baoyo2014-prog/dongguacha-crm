# 冬瓜茶客戶追蹤 — Google Sheets 同步設定指南

這份指南教你如何把「冬瓜茶客戶追蹤」連上你的 Google 試算表，讓**不同電腦/手機/多人**都能共享同一份客戶資料。

---

## 你需要準備
- 一個 **Google 帳號**（免費）
- 瀏覽器

---

## 第一步：建立 Google 試算表

1. 開啟 https://sheets.new （會自動建立一個新試算表）
2. 把它改名，例如「冬瓜茶客戶追蹤」
3. 不需要手動做任何事，程式會自動建立「資料」分頁

---

## 第二步：加入 Apps Script

1. 在試算表中，點上方選單 **「擴充功能 → Apps Script」**
2. 會開一個新的程式編輯器分頁
3. **刪掉**編輯器裡原本預設的 `function myFunction() {...}` 全部內容
4. 把檔案 `google-sheets-sync.gs` 的**全部內容**複製貼上
5. 按上方工具列的 **「儲存」**（💾 圖示）

---

## 第三步：部署為 Web App

1. 在 Apps Script 編輯器右上角，按 **「部署 → 新增部署作業」**
2. 類型選 **「網頁應用程式 (Web app)」**
3. 「說明」可隨意填，例如「客戶追蹤同步」
4. 「執行身分」選 **你（你的 Google 帳號）**
5. 「誰可以存取」：**選「Anyone」（任何人）** ← 很重要，這樣別人才讀得到
6. 按 **「部署」**
7. 第一次會要求**授權**：按「授權存取」→ 選你的帳號 → 「進階 → 前往專案（不安全）」→「允許」
8. 部署完成後，會出現一個 **「網頁應用程式網址 (Web app URL)」**，長這樣：
   ```
   https://script.google.com/macros/s/AKfycb.../exec
   ```
   **把它複製起來，這就是你的「同步網址」。**

> 💡 將來若改了 gs 程式碼，要按「部署 → 管理部署作業 → 編輯 → 版本選『新版本』→ 部署」，網址會變或不變（視設定）。

---

## 第四步：把同步網址填入你的 index.html

開啟 `index.html`，找到這一行（接近程式碼頂端）：

```js
const GAS_URL = '';   // ← 把網址填到這裡
```

改成：

```js
const GAS_URL = 'https://script.google.com/macros/s/AKfycb.../exec';
```

儲存。

---

## 第五步：確認同步功能

重新整理 index.html，右上角應該會出現一個 **「☁️ 雲同步」** 按鈕。點它可立即：
- **拉取** — 從 Google 表讀最新資料
- **推送** — 把本機資料寫到 Google 表

開啟程式時會**自動**嘗試同步一次。

---

## 多人共用方式

每個人只需要：

1. 拿到你的 `index.html`（含同步網址），放上網路（例如 **GitHub Pages**）或傳給大家各自的電腦
2. 各自在瀏覽器開啟
3. 開啟時自動拉取最新資料，改完自動推送

> ⚠️ **重要**：這類 HTML 檔若用「檔案總管直接雙擊」開啟，網址是 `file://`，瀏覽器**無法跨網域呼叫** Google Apps Script。為了多人共用，建議把 `index.html` 放到網路空間（如 GitHub Pages、Netlify、或任何靜態網站託管）。

---

## 資料同步規則（請了解）

- **本機優先**：程式先在本機 localStorage 存一份（離線也能用），儲存後再自動推送雲端。
- **以「最後更新時間」取捨**：多人同時改不同人，最後寫入者會覆蓋。這是 Google Sheets 免費方案的先天限制，請注意**避免多人同時編輯同一筆**。
- **不是即時多人協作**：是「開啟時拉取、儲存後推送」的同步方式，有幾秒到幾十秒延遲。

---

## 常見問題

**Q：點同步沒反應？**
確認 GAS_URL 有正確填入，且 Apps Script 已部署為「任何人」可存取。

**Q：出現 CORS 或 401 錯誤？**
確認部署「誰可以存取」= Anyone，且你已授權。

**Q：別人的電腦打不開我的 index.html？**
因為你是 `file://` 開啟。需把 index.html 上傳到網路託管，再把網址給對方。

**Q：我改了 gs 程式但雲端沒變新？**
部署新版（第三步的第 8 點提示）。

**Q：資料會衝突嗎？**
多人都改同一客戶、且最後推送者覆蓋。建議一人維護某幾家客戶，避免同時改同一個。
